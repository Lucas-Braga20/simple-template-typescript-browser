{
  "name": "simple-template-typescript-browser",
  "version": "1.0.0",
  "description": "Simple template using typescript for browser.",
  "scripts": {
    "build:watch": "start webpack --mode development -d source-map --progress --watch && start npx live-server",
    "build:dev": "start webpack --mode development -d source-map --progress && start npx live-server",
    "build:prod": "webpack --mode production --progress && start npx live-server"
  },
  "author": "Lucas Braga",
  "license": "ISC",
  "devDependencies": {
    "babel-loader": "^8.2.3",
    "live-server": "^1.2.1",
    "ts-loader": "^9.2.8",
    "typescript": "^3.8.3",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  }
}
